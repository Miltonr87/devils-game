import React from 'react';

const GameContent = ({ 
    image,
    name,
    date,
    rating }) => {

    return (
        <div>
            {name}
        </div>
    )
};

export default GameContent;